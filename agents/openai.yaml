interface:
  display_name: "Sol to Luna Local Parallel"
  short_description: "Reuse local Luna tasks for safe parallel work"
  default_prompt: "Use $sol-luna-local-parallel to reuse Luna tasks for independent parallel work."

policy:
  allow_implicit_invocation: true
